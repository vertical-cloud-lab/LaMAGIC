# Dependencies for the basic test suite (tests/).
#
# The unit tests in tests/test_topo_graph.py only need numpy + pytest.
# The end-to-end tests in tests/test_lamagic_pipeline.py additionally exercise
# the real released dataset/model, so they need the transformers stack.
#
# A CPU-only torch build is sufficient:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# The ngspice verification test (test_ngspice_simulation_closes_the_loop)
# additionally needs the system `ngspice` binary, which is not pip-installable:
#   sudo apt-get install -y ngspice    # (or: conda install -c conda-forge ngspice)
pytest
numpy
networkx
huggingface_hub
transformers==4.36.0
tokenizers==0.15.2
sentencepiece
protobuf
torch
matplotlib
